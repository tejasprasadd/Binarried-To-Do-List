{
  "name": "binaried",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.2.0",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "bun --filter server run dev & bun --filter client run start",
    "dev:client": "bun --filter client run start",
    "dev:server": "bun --filter server run dev",
    "build": "bun --filter client run build",
    "test": "bun --filter server run test && bun --filter client run test",
    "typecheck": "bun --filter server run typecheck && bun --filter client run typecheck"
  }
}
