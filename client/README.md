# Binaried client

Angular standalone frontend for Binaried. Run it from the repository root with `bun run dev:client`, or use `bun run --cwd client build` and `bun run --cwd client test` for local verification.

See the [root README](../README.md) for complete setup and delivery notes.
